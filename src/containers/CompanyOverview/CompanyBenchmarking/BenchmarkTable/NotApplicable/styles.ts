import { StyledToolTip } from 'components/InfoToolTip/styledComponents';
import styled from 'styled-components';

export const Container = styled(StyledToolTip)`
  .__react_component_tooltip {
    box-shadow: none !important;
  }
`;
