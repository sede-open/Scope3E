export const getCommunitySpaceCardId = (id: number) =>
  `most-popular-spaces-cards[${id}]`;
export const getCommunitySpaceCardImageSrc = (id: number) =>
  `/images/CommunityImages/CommunitySpaceCardImages/space-card[${id}].png`;
