import styled from 'styled-components';

import { Gray } from 'styles/colours';

export const NoOptionsContainer = styled.div`
  padding: 0.625rem 1rem;
`;

export const NoOptionsParagraph = styled.p`
  color: ${Gray};
  font-size: 0.875rem;
`;
